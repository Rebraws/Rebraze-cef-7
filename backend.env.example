# Backend Environment Configuration for CEF OAuth
# Copy this to /home/rebraws/Rebraze/Mutiny/src/backend/.env

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-this-in-production
JWT_EXPIRATION_HOURS=24

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Backend OAuth callback (where Google redirects after user authorizes)
# This should match the redirect URI in your Google Cloud Console
GOOGLE_REDIRECT_URI=http://0.0.0.0:8000/auth/google/callback

# Frontend URL (where backend redirects with the token)
# FOR CEF APP: Use http://localhost:8765 (CEF OAuth server port)
# FOR WEB DEV: Use http://localhost:5173 (Vite dev server)
FRONTEND_URL=http://localhost:8765
